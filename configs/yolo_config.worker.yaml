model:
  # Ultralytics downloads this checkpoint on first use when it is not present.
  path: yolo26m.pt
  device: cuda:0
detection:
  conf_threshold: 0.5
  iou_threshold: 0.7
  classes:
  - 0
  imgsz: 640
inference:
  half: true
  augment: false
  agnostic_nms: false
  max_det: 50
