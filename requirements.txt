# Core dependencies for YOLO + TAO ReID + Triton Pipeline

# Deep Learning Frameworks
#torch>=2.2.1
#torchvision>=0.17.1
#tensorrt>=8.6.0 # Seriosly problematic with pip
# Required only for backend: tensorrt_direct on Jetson with CUDA 12.5.
cuda-python==12.5.0

# Triton Inference Client
tritonclient[all]>=2.64.0

# Model Export & Deployment
onnx>=1.20.0
# Jetson/aarch64 requires NVIDIA's JetPack-compatible ONNX Runtime GPU wheel.
# The PyPI GPU wheel below is for the local x86_64 CUDA environment.
onnxruntime-gpu>=1.23.0,<1.24.0; platform_machine == "x86_64"

# YOLO Detection
ultralytics>=8.4.0,<9.0.0

# Multi-Object Tracking
boxmot>=16.0.0,<17.0.0

# Image Processing
opencv-python>=4.7.0
pillow>=9.0.0

# System Monitoring
psutil>=5.9.0
GPUtil>=1.4.0

# Configuration & Logging
pyyaml>=6.0
loguru>=0.7.0

# Data Processing
#numpy>=1.21.0
pandas>=2.0.0

# Utilities
tqdm>=4.64.0
click>=8.1.0
aiohttp>=3.9.0

# Optional: For development
ipython
jupyter
matplotlib
